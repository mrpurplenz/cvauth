
APP_NAME = "cvauth"
CONFIG_FILENAME = "cvauth.toml"
DEFAULT_CONFIG_TEMPLATE_PATH = Path(__file__).with_name(CONFIG_FILENAME)

suffix PATH directory and file name
suffix FILE file name only
suffix DIR  directory only



CONFIG_TEMPLATE_FILE name of the package config template file = cvauth.toml
CONFIG_TEMPLATE_PATH 
CONFIG_DEFAULT_FILE
CONFIG_DEFAULT_PATH
DEFAULT_CONFIG toml bytes of the default template config

CONFIG_FILE
CONFIG_PATH

PRIVATE_KEY_DEFAULT_PATH
PRIVATE_KEY_PATH
PRIVATE_KEY_FILE

PUBLIC_KEY_DEFAULT_PATH
PUBLIC_KEY_PATH
PUBLIC_KEY_FILE
